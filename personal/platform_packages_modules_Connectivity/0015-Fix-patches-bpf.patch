From 6c624dab855a00d9cd56319f524f6db15346816b Mon Sep 17 00:00:00 2001
From: Doze-off <doze-off@doze-off>
Date: Fri, 18 Jul 2025 08:40:32 +0000
Subject: [PATCH 15/15] Fix patches  bpf

---
 bpf/headers/include/bpf/BpfMap.h |  5 +----
 bpf/loader/NetBpfLoad.cpp        | 11 +++++------
 2 files changed, 6 insertions(+), 10 deletions(-)

diff --git a/bpf/headers/include/bpf/BpfMap.h b/bpf/headers/include/bpf/BpfMap.h
index 2216373..4da3b53 100644
--- a/bpf/headers/include/bpf/BpfMap.h
+++ b/bpf/headers/include/bpf/BpfMap.h
@@ -41,9 +41,8 @@ using std::function;
 #define BPFMAP_VERBOSE_ABORT
 #endif
 
-[[noreturn]] __attribute__((__format__(__printf__, 2, 3))) static inline
+__attribute__((__format__(__printf__, 2, 3))) static inline
 void Abort(int __unused error, const char* __unused fmt, ...) {
-#ifdef BPFMAP_VERBOSE_ABORT
     va_list va;
     va_start(va, fmt);
 
@@ -54,9 +53,7 @@ void Abort(int __unused error, const char* __unused fmt, ...) {
     fflush(stderr);
 
     va_end(va);
-#endif
 
-    abort();
 }
 
 
diff --git a/bpf/loader/NetBpfLoad.cpp b/bpf/loader/NetBpfLoad.cpp
index 5ab6bd3..b994369 100644
--- a/bpf/loader/NetBpfLoad.cpp
+++ b/bpf/loader/NetBpfLoad.cpp
@@ -1699,27 +1699,27 @@ static int doLoad(char** argv, char * const envp[]) {
 
     // both S and T require kernel 4.9 (and eBpf support)
     if (!isAtLeastKernelVersion(4, 9, 0)) {
-        ALOGW("Android S & T require kernel 4.9.");
+        ALOGE("Android S & T require kernel 4.9.");
         failed = true;
     }
 
     // U bumps the kernel requirement up to 4.14
     if (isAtLeastU && !isAtLeastKernelVersion(4, 14, 0)) {
-        ALOGW("Android U requires kernel 4.14.");
+        ALOGE("Android U requires kernel 4.14.");
         failed = true;
     }
 
     // V bumps the kernel requirement up to 4.19
     // see also: //system/netd/tests/kernel_test.cpp TestKernel419
     if (isAtLeastV && !isAtLeastKernelVersion(4, 19, 0)) {
-        ALOGW("Android V requires kernel 4.19.");
+        ALOGE("Android V requires kernel 4.19.");
         failed = true;
     }
 
     // 25Q2 bumps the kernel requirement up to 5.4
     // see also: //system/netd/tests/kernel_test.cpp TestKernel54
     if (isAtLeast25Q2 && !isAtLeastKernelVersion(5, 4, 0)) {
-        ALOGW("Android 25Q2 requires kernel 5.4.");
+        ALOGE("Android 25Q2 requires kernel 5.4.");
         failed = true;
     }
 
@@ -1852,8 +1852,7 @@ static int doLoad(char** argv, char * const envp[]) {
         // but we need 0 (enabled)
         // (this writeFile is known to fail on at least 4.19, but always defaults to 0 on
         // pre-5.13, on 5.13+ it depends on CONFIG_BPF_UNPRIV_DEFAULT_OFF)
-        if (writeProcSysFile("/proc/sys/kernel/unprivileged_bpf_disabled", "0\n") &&
-            isAtLeastKernelVersion(5, 13, 0)) {
+        if (writeProcSysFile("/proc/sys/kernel/unprivileged_bpf_disabled", "0\n")) {
             failed = true;
         }
     }
-- 
2.43.0

