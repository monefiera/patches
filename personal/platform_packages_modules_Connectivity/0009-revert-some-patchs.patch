From 40349e79cdb3548ce7c2463e08e428f60903c1c6 Mon Sep 17 00:00:00 2001
From: papacu <papacu@papacu>
Date: Fri, 13 Jun 2025 07:05:00 +0000
Subject: [PATCH 9/9] revert some patchs

---
 bpf/loader/NetBpfLoad.cpp                      | 3 ---
 service/src/com/android/server/BpfNetMaps.java | 2 +-
 2 files changed, 1 insertion(+), 4 deletions(-)

diff --git a/bpf/loader/NetBpfLoad.cpp b/bpf/loader/NetBpfLoad.cpp
index 414df92296..f8c8913330 100644
--- a/bpf/loader/NetBpfLoad.cpp
+++ b/bpf/loader/NetBpfLoad.cpp
@@ -1697,20 +1697,17 @@ static int doLoad(char** argv, char * const envp[]) {
     // T require kernel 4.9 (and eBpf support)
     if (isAtLeastT && !isAtLeastKernelVersion(4, 9, 0)) {
         ALOGW("Android T requires kernel 4.9.");
-        failed = true;
     }
 
     // U bumps the kernel requirement up to 4.14
     if (isAtLeastU && !isAtLeastKernelVersion(4, 14, 0)) {
         ALOGW("Android U requires kernel 4.14.");
-        failed = true;
     }
 
     // V bumps the kernel requirement up to 4.19
     // see also: //system/netd/tests/kernel_test.cpp TestKernel419
     if (isAtLeastV && !isAtLeastKernelVersion(4, 19, 0)) {
         ALOGW("Android V requires kernel 4.19.");
-        failed = true;
     }
 
     // 25Q2 bumps the kernel requirement up to 5.4
diff --git a/service/src/com/android/server/BpfNetMaps.java b/service/src/com/android/server/BpfNetMaps.java
index 332d5f2c58..92f00273e3 100644
--- a/service/src/com/android/server/BpfNetMaps.java
+++ b/service/src/com/android/server/BpfNetMaps.java
@@ -1067,7 +1067,7 @@ public class BpfNetMaps {
     @RequiresApi(Build.VERSION_CODES.TIRAMISU)
     public int getNetPermForUid(final int uid) {
         final int appId = UserHandle.getAppId(uid);
-        if (sUidPermissionMap == null) return PERMISSION_INTERNET;
+        // if (sUidPermissionMap == null) return;
         try {
             // Key of uid permission map is appId
             // TODO: Rename map name
-- 
2.43.0

